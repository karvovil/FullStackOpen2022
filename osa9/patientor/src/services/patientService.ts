import patientData from '../../data/patients.json';

const getPatients = () => {
  return patientData;
};

const addPatient = () => {
  return null;
};

export default {
  getPatients,
  addPatient
};
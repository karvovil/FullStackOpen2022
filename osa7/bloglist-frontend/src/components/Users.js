const Users = () => {
  //const users = 
return (<p>This is Lusers component</p>)
/*   return users.map( user => (
    <div>
      {user.name} {user.blogs.length}
    </div>
  ))
  */

} 
export default Users